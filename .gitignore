__pycache__/
*.py[cod]
.env

# Datasets
data/

# Large artifacts (downloaded HF snapshots, OCR predictions, feature dumps)
models/
predictions/
# Archived DiT/DINO + cluster pipeline (features, results, scripts — keep locally)
legacy/

# Per-image score CSVs and result outputs
models/*.csv
notebooks/.ipynb_checkpoints/

# Virtualenvs
.venv/
.venv-*/

# Shell scripts (contain machine-specific paths)
*.sh

# HF / torch caches if accidentally created in-repo
.cache/
.locks/
*.TAG
logs/

.venv-vllm/
# New analyses at repo root (optional)
results/*.csv
results/*.json

rankshift_save_2026-04-28.tgz
.env.example
paper/
results/